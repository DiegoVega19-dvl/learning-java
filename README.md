# 📘 Guía de Estudio – Programación II (POO en Java)

## 📂 Unidades de Estudio

### 🔹 Unidad 1: Fundamentos del lenguaje y estándares de codificación
- Sintaxis básica de Java.
- Comentarios, identificadores y palabras reservadas.
- Tipos de datos básicos (`int`, `double`, `char`, `boolean`).
- Variables y constantes.
- Expresiones y operadores.
- Sentencias de flujo de control (`if`, `else`, `switch`, `for`, `while`, `do-while`).

- estudiar del viernes 5 de septiembre al viernes 12 de septiembre

---

### 🔹 Unidad 2: Introducción a la Programación Orientada a Objetos (POO)
- Clases, objetos y métodos.
- Constructores (`default` y con parámetros).
- Modificadores de acceso (`public`, `private`, `protected`).
- Uso de `this`.

---

### 🔹 Unidad 3: Arreglos y cadenas
- Arreglos de tipos primitivos.
- Arreglos de objetos.
- Manejo de cadenas (`String` y métodos como `.length()`, `.substring()`, `.equals()`).

---

### 🔹 Unidad 4: Principios avanzados de la POO
- **Herencia** (`extends`, `super`).
- **Polimorfismo** (sobrescritura de métodos, uso de referencias padre → objeto hijo).
- **Abstracción** (clases abstractas, métodos abstractos).
- Ventajas de la POO.

---

### 🔹 Unidad 5: Interfaces
- Definición de interfaces (`interface`).
- Implementación de interfaces en clases.
- Diferencias entre interfaz y clase abstracta.

---

## 🛠️ Prácticas Clave

### ✅ 1. Tipos de datos básicos y operaciones matemáticas
**Ejercicio sugerido:**  
Programa que lea 3 números y muestre:
- El mayor y el menor.  
- El promedio.  

---

### ✅ 2. Estructuras de control
**Ejercicio sugerido:**  
Menú con `switch`:  
1. Calcular el área de un círculo.  
2. Calcular el factorial de un número.  
3. Imprimir la tabla de multiplicar de un número.  

---

### ✅ 3. Definición de clases, objetos y métodos
**Ejercicio sugerido:**  
Clase `Estudiante` con:
- Atributos: `nombre`, `edad`, `calificacion`.  
- Método `aprobo()` que devuelva `true/false`.  

En `main`, crear 3 estudiantes y mostrar si aprobaron.

---

### ✅ 4. Herencia
**Ejercicio sugerido:**  
Clase `Vehiculo` con atributos `marca` y `velocidadMax`.  
Subclases: `Auto` y `Moto`.  
Método sobrescrito `mostrarInfo()`.  

---

### ✅ 5. Abstracción y Polimorfismo
**Ejercicio sugerido:**  
Clase abstracta `Figura` con método abstracto `calcularArea()`.  
Subclases: `Rectangulo` y `Triangulo`.  
En `main`, crear un arreglo de `Figura` y recorrerlo para calcular áreas.  

---

### ✅ 6. Interfaces
**Ejercicio sugerido:**  
Interfaz `Operaciones` con método `calcular()`.  
Clases `Suma` y `Resta` que implementen la interfaz.  
En `main`, probar las operaciones.  

---

## 🗓️ Estrategia de Estudio (4 semanas)

- **Semana 1:**  
  - Tipos de datos y operadores.  
  - Condicionales y ciclos.  
  - Prácticas con menús y operaciones matemáticas.  

- **Semana 2:**  
  - Clases, objetos, constructores y métodos.  
  - Ejercicios de modelado de objetos (`Persona`, `Estudiante`, etc.).  

- **Semana 3:**  
  - Herencia + polimorfismo + abstracción.  
  - Ejercicios con jerarquías de clases (`Animal`, `Figura`, etc.).  

- **Semana 4:**  
  - Interfaces.  
  - Repaso con mini-proyectos que mezclen todo lo anterior.  
